#include<bits/stdc++.h>

using namespace std;

int main()
{
  //Installize
  int n;
  string s[10];
  //Input
  scanf("%d", &n);
  for(int i=1; i<=n; i++) {
    cin >> s[i];
  }
  //Test Input
  cout << n;
  for(int i=1; i<=n;i++){
    cout << s[i];
  }
  return 0;
}
